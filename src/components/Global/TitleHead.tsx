const TitleHead = ({ title }: any) => {
    return (
        <div>
            <h1 className="text-white text-2xl font-bold px-[5px] pb-4"> {title}</h1>
        </div>
    );
};

export default TitleHead;
